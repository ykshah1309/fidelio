# Node
node_modules/
npm-debug.log*
yarn-debug.log*

# Next.js
.next/
out/
build/
dist/
*.tsbuildinfo
next-env.d.ts

# Env — never commit secrets
.env
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/settings.json
.idea/

# Vercel
.vercel

# Sample PDFs that may contain PII — but whitelist the synthetic samples
# generated by `npm run samples` (fictional data only, safe to commit).
app/public/samples/*.pdf
!app/public/samples/.gitkeep
!app/public/samples/sample-401k.pdf
!app/public/samples/sample-paystub.pdf

# Uploads / temp
tmp-uploads/
tmp-reports/
